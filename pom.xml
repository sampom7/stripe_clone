<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.5</version>
        <relativePath/>
    </parent>

    <groupId>com.stripeclone</groupId>
    <artifactId>stripe-clone</artifactId>
    <version>2.0.0</version>
    <name>Stripe Clone</name>
    <description>Stripe's payments API over a double-entry ledger</description>

    <properties>
        <java.version>21</java.version>
        <!-- Empty on Linux/macOS so Testcontainers uses its own discovery. -->
        <docker.host></docker.host>
    </properties>

    <profiles>
        <profile>
            <id>windows-docker-desktop</id>
            <activation>
                <os><family>windows</family></os>
            </activation>
            <properties>
                <docker.host>npipe:////./pipe/dockerDesktopLinuxEngine</docker.host>
            </properties>
        </profile>
    </profiles>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <systemPropertyVariables>
                        <!--
                          Docker Engine 29 requires API 1.40 or newer, but the docker-java
                          client bundled with Testcontainers 1.21 still negotiates 1.32. The
                          engine answers that with a bare HTTP 400, which Testcontainers
                          reports only as "Could not find a valid Docker environment" - so
                          the symptom looks like Docker is absent when it is merely too new.
                        -->
                        <api.version>1.44</api.version>
                    </systemPropertyVariables>
                    <environmentVariables>
                        <!--
                          Docker Desktop on Windows serves its engine on a named pipe that
                          Testcontainers does not probe by default. Resolving it here means
                          the build works without the developer exporting DOCKER_HOST.
                          Empty and therefore ignored on Linux and macOS, where
                          Testcontainers' own discovery already works.
                        -->
                        <DOCKER_HOST>${docker.host}</DOCKER_HOST>
                    </environmentVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
