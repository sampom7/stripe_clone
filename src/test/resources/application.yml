spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
  threads:
    virtual:
      enabled: true

logging:
  level:
    root: WARN
    com.stripeclone: INFO
    org.testcontainers: WARN
