# Reuse the Postgres container between runs so repeated local runs stay fast. Also needs
# testcontainers.reuse.enable=true in ~/.testcontainers.properties to take effect.
testcontainers.reuse.enable=true
